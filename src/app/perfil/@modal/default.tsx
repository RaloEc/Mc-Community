/**
 * Default slot para @modal
 * Retorna null cuando no hay modal activo
 * Se renderiza cuando la ruta no coincide con ninguna ruta interceptada
 */

export default function Default() {
  return null;
}
